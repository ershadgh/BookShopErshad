@using Microsoft.AspNetCore.Identity
@using BookShop.Areas.Identity.Data
@inject SignInManager<ApplicationUser > SignInManager
@inject UserManager<ApplicationUser > UserManager

@if (SignInManager.IsSignedIn(User))
{
  //  <form asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Page("/Index", new { area = "" })" method="post" id="logoutForm" class="navbar-right">
      <form asp-action="SignOut" asp-controller="Account" method="post" id="logoutForm" class="navbar-right">
        @*<ul class="nav navbar-nav navbar-right">*@
            <ul class="navbar-nav mt-2 mt-lg-0">
            <li>
                @*<a asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>*@
                  <a  title="Manage" class="nav-link ">سلام @UserManager.GetUserName(User)!</a>
            </li>
            <li>
                <button type="submit" class="btn btn-primary  navbar-link nav-link ">خروج</button>
            </li>
            <li>
                @if (User.IsInRole("کاربر1")|| User.IsInRole("مشتری"))
                {
                    <a asp-action="ChangePassword" asp-controller="Account" class="nav-link">ورود به پنل کاربرب</a>
                }
                else
                {
                    <a href="/Admin/Dashboard/Index" class="nav-link">ورد به پنل مدیریت</a>
                }
            </li>
        </ul>
    </form>
}
else
{
    
     <ul class="nav navbar-nav navbar-right">
        <li ><a asp-action="Register" asp-controller="Account" class="nav-link ">عضویت</a></li>
        <li  ><a asp-action="SignIn" asp-controller="Account" class="nav-link ">ورود به سایت</a></li>
    </ul>
}
